<html lang='en' dir='ltr'>
  <head>
    <meta charset='UTF-8' />
    <meta name='viewport' content='width=device-width, initial-scale=1.0' />
    <meta name='color-scheme' content='light' />
    <meta name='supported-color-schemes' content='light' />
    <title>Eureka Enterprises - Vehicle Document Expiry Alert</title>
    <style>
      body { margin: 0; padding: 0; width: 100%; font-family: 'Segoe UI', Tahoma, Geneva, Verdana,
      sans-serif; font-size: 15px; line-height: 1.6; color: #2d3748; background-color: #f7fafc; }
      .email-wrapper { padding: 40px 20px; background-color: #f7fafc; }
      .email-container { max-width: 700px; margin: 0 auto; background-color: #ffffff; border-radius: 8px;
      box-shadow: 0 4px 6px rgba(0, 0, 0, 0.07); overflow: hidden; }
      .header { background: linear-gradient(135deg, #1a365d 0%, #2c5282 100%); padding: 32px 40px;
      text-align: center; }
      .header-icon { font-size: 48px; margin-bottom: 12px; }
      .logo { max-width: 120px; height: auto; display: block; margin: 0 auto 16px; border-radius: 8px; }
      .company-name { color: #ffffff; font-size: 22px; font-weight: 600; margin: 0; letter-spacing: 0.5px; }
      .content { padding: 40px; }
      .greeting { font-size: 16px; color: #1a365d; margin-bottom: 20px; }
      .message-text { color: #4a5568; margin-bottom: 24px; line-height: 1.7; }
      
      /* Summary Cards */
      .summary-container { display: flex; gap: 16px; margin: 24px 0; }
      .summary-card { flex: 1; padding: 20px; border-radius: 8px; text-align: center; }
      .summary-card.expired { background: linear-gradient(135deg, #fed7d7 0%, #feb2b2 100%);
      border: 1px solid #fc8181; }
      .summary-card.expiring { background: linear-gradient(135deg, #fef3c7 0%, #fde68a 100%);
      border: 1px solid #f59e0b; }
      .summary-count { font-size: 42px; font-weight: 700; line-height: 1; }
      .summary-card.expired .summary-count { color: #c53030; }
      .summary-card.expiring .summary-count { color: #92400e; }
      .summary-label { font-size: 12px; font-weight: 600; text-transform: uppercase; letter-spacing: 1px;
      margin-top: 8px; }
      .summary-card.expired .summary-label { color: #c53030; }
      .summary-card.expiring .summary-label { color: #92400e; }
      
      /* Section Headers */
      .section-header { display: flex; align-items: center; gap: 12px; margin: 32px 0 16px 0; }
      .section-header.expired { color: #c53030; }
      .section-header.expiring { color: #92400e; }
      .section-icon { width: 28px; height: 28px; border-radius: 50%; display: flex; align-items: center;
      justify-content: center; font-size: 14px; font-weight: bold; }
      .section-header.expired .section-icon { background-color: #fed7d7; color: #c53030; }
      .section-header.expiring .section-icon { background-color: #fef3c7; color: #92400e; }
      .section-title { font-size: 16px; font-weight: 600; margin: 0; }
      
      /* Vehicle Cards */
      .vehicle-card { background-color: #f7fafc; border: 1px solid #e2e8f0; border-radius: 8px;
      margin-bottom: 16px; overflow: hidden; }
      .vehicle-card.expired { border-left: 4px solid #c53030; }
      .vehicle-card.expiring { border-left: 4px solid #f59e0b; }
      .vehicle-header { padding: 16px 20px; background-color: #edf2f7; border-bottom: 1px solid #e2e8f0; }
      .vehicle-reg { font-size: 18px; font-weight: 700; color: #1a365d; margin: 0 0 4px 0; }
      .vehicle-info { font-size: 13px; color: #718096; margin: 0; }
      .vehicle-body { padding: 16px 20px; }
      
      /* Document Row */
      .document-row { display: flex; align-items: center; justify-content: space-between;
      padding: 12px 0; border-bottom: 1px solid #e2e8f0; }
      .document-row:last-child { border-bottom: none; }
      .document-info { flex: 1; }
      .document-type { font-size: 14px; font-weight: 600; color: #2d3748; margin: 0 0 2px 0; }
      .document-date { font-size: 12px; color: #718096; margin: 0; }
      .document-status { padding: 4px 12px; border-radius: 12px; font-size: 11px; font-weight: 600;
      text-transform: uppercase; letter-spacing: 0.5px; }
      .document-status.expired { background-color: #fed7d7; color: #c53030; }
      .document-status.expiring-soon { background-color: #fef3c7; color: #92400e; }
      
      /* Legend */
      .legend { display: flex; gap: 24px; margin: 24px 0; padding: 16px 20px; background-color: #f7fafc;
      border-radius: 6px; }
      .legend-item { display: flex; align-items: center; gap: 8px; font-size: 12px; color: #4a5568; }
      .legend-dot { width: 12px; height: 12px; border-radius: 50%; }
      .legend-dot.expired { background-color: #c53030; }
      .legend-dot.expiring { background-color: #f59e0b; }
      
      /* Button */
      .button-container { text-align: center; margin: 32px 0; }
      .button { display: inline-block; padding: 14px 32px; background: linear-gradient(135deg, #2c5282 0%, #1a365d 100%);
      color: #ffffff !important; text-decoration: none; border-radius: 6px; font-weight: 600;
      font-size: 14px; letter-spacing: 0.5px; }
      
      .divider { height: 1px; background-color: #e2e8f0; margin: 28px 0; }
      .signature { margin-top: 28px; color: #4a5568; }
      .signature-name { color: #1a365d; font-weight: 600; }
      .footer { background-color: #f7fafc; text-align: center; padding: 24px 40px;
      border-top: 1px solid #e2e8f0; }
      .footer-text { color: #718096; font-size: 12px; margin: 0 0 8px 0; }
      .footer-company { color: #a0aec0; font-size: 11px; margin: 0; }
      
      @media screen and (max-width: 600px) {
        .content { padding: 28px 24px; }
        .header { padding: 24px; }
        .footer { padding: 20px 24px; }
        .summary-container { flex-direction: column; }
        .summary-count { font-size: 32px; }
        .vehicle-header { padding: 12px 16px; }
        .vehicle-body { padding: 12px 16px; }
        .document-row { flex-direction: column; align-items: flex-start; gap: 8px; }
        .legend { flex-direction: column; gap: 12px; }
      }
    </style>
  </head>
  <body>
    <div class='email-wrapper'>
      <div class='email-container'>
        <div class='header'>
          <img
            src='https://5.imimg.com/data5/AY/VX/BQ/SELLER-107173792/vvv-120x120.jpeg'
            alt='Eureka Enterprises'
            class='logo'
          />
          <p class='company-name'>Eureka Enterprises</p>
        </div>

        <div class='content'>
          <p class='greeting'><strong>Vehicle Document Expiry Alert</strong></p>

          <p class='message-text'>
            This is an automated alert regarding vehicle documents that require immediate attention.
            Please review and renew the following documents to ensure compliance and avoid penalties.
          </p>

          <!-- Summary Cards -->
          <table width="100%" cellpadding="0" cellspacing="0" style="margin: 24px 0;">
            <tr>
              {{#if hasExpired}}
              <td width="48%" style="padding-right: 8px;">
                <div class='summary-card expired'>
                  <div class='summary-count'>{{totalExpired}}</div>
                  <div class='summary-label'>Documents Expired</div>
                </div>
              </td>
              {{/if}}
              {{#if hasExpiringSoon}}
              <td width="{{#if hasExpired}}48%{{else}}100%{{/if}}" style="{{#if hasExpired}}padding-left: 8px;{{/if}}">
                <div class='summary-card expiring'>
                  <div class='summary-count'>{{totalExpiringSoon}}</div>
                  <div class='summary-label'>Expiring Soon</div>
                </div>
              </td>
              {{/if}}
            </tr>
          </table>

          <!-- Legend -->
          <div class='legend'>
            <div class='legend-item'>
              <span class='legend-dot expired'></span>
              <span>Expired - Requires immediate action</span>
            </div>
            <div class='legend-item'>
              <span class='legend-dot expiring'></span>
              <span>Expiring Soon - Plan renewal</span>
            </div>
          </div>

          {{#if hasExpired}}
          <!-- Expired Documents Section -->
          <div class='section-header expired'>
            <div class='section-icon'>!</div>
            <h3 class='section-title'>Expired Documents ({{totalExpired}})</h3>
          </div>

          {{#each expiredVehicles}}
          <div class='vehicle-card expired'>
            <div class='vehicle-header'>
              <p class='vehicle-reg'>{{registrationNo}}</p>
              <p class='vehicle-info'>{{brand}} {{model}} • {{vehicleNumber}} • Assigned: {{assignedTo}}</p>
            </div>
            <div class='vehicle-body'>
              {{#each documents}}
              <div class='document-row'>
                <div class='document-info'>
                  <p class='document-type'>{{label}}</p>
                  <p class='document-date'>Expired on {{endDate}}</p>
                </div>
                <span class='document-status {{statusClass}}'>{{daysText}}</span>
              </div>
              {{/each}}
            </div>
          </div>
          {{/each}}
          {{/if}}

          {{#if hasExpiringSoon}}
          <!-- Expiring Soon Section -->
          <div class='section-header expiring'>
            <div class='section-icon'>⚠</div>
            <h3 class='section-title'>Expiring Soon ({{totalExpiringSoon}})</h3>
          </div>

          {{#each expiringSoonVehicles}}
          <div class='vehicle-card expiring'>
            <div class='vehicle-header'>
              <p class='vehicle-reg'>{{registrationNo}}</p>
              <p class='vehicle-info'>{{brand}} {{model}} • {{vehicleNumber}} • Assigned: {{assignedTo}}</p>
            </div>
            <div class='vehicle-body'>
              {{#each documents}}
              <div class='document-row'>
                <div class='document-info'>
                  <p class='document-type'>{{label}}</p>
                  <p class='document-date'>Expires on {{endDate}}</p>
                </div>
                <span class='document-status {{statusClass}}'>{{daysText}}</span>
              </div>
              {{/each}}
            </div>
          </div>
          {{/each}}
          {{/if}}

          <div class='button-container'>
            <a href='{{adminPortalUrl}}' class='button'>View Fleet Dashboard</a>
          </div>

          <div class='divider'></div>

          <p class='message-text' style='font-size: 13px;'>
            <strong>Note:</strong> Driving with expired documents may result in:
          </p>
          <ul style='color: #4a5568; font-size: 13px; margin: 0 0 20px 0; padding-left: 20px;'>
            <li>Heavy fines and penalties</li>
            <li>Vehicle impoundment</li>
            <li>Insurance claim rejection</li>
            <li>Legal complications</li>
          </ul>

          <div class='signature'>
            <p>
              Best regards,<br />
              <span class='signature-name'>Eureka HRMS</span>
            </p>
          </div>
        </div>

        <div class='footer'>
          <p class='footer-text'>This is an automated alert. Please do not reply directly to this email.</p>
          <p class='footer-company'>&copy; {{currentYear}} Eureka Enterprises. All rights reserved.</p>
        </div>
      </div>
    </div>
  </body>
</html>
